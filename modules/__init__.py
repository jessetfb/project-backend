import sys
import os

# Assuming 'Modals' is located in the same directory as 'main.py'
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), 'modules')))

# Now you can import your modulefrom properties import Property
